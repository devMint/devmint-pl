name: Continous Integration

on:
  pull_request:
  push:
    branches:
      - master

jobs:
  test_css:
    name: Lint, test and validate for CSS
    uses: ./.github/workflows/workflow_test_css.yml

  test_js:
    name: Lint, test and type-check for JS/TS
    uses: ./.github/workflows/workflow_test_js.yml
