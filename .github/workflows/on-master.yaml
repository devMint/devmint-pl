name: 🧪 Check JS/TS

on:
  push:
    branches:
      - master

permissions:
  id-token: write
  contents: read
  deployments: write

jobs:
  test_lint:
    name: 🧪 Verify code changes
    uses: ./.github/workflows/workflow-lint.yaml
    with:
      ESLINT_REPORTER: 'github-check'
